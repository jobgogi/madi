// prompt_templates에서 관리자가 편집하는 내용 뒤에 항상 고정으로 붙는 규칙
// (카테고리 enum, 배치 문장 처리, 출력 형식 지시). assertReportCount 등 코드의
// 구조적 제약과 직결돼 있어 관리자가 자유 편집하지 못하도록 DB 밖에 둔다
// (2026-09-11 사용자 요청). lib/providers/analyze.ts(서버 전용 LLM SDK를
// top-level import)가 아니라 이 작은 파일에 둔 이유: 관리자 화면(클라이언트
// 컴포넌트)에서 고정 규칙을 읽기 전용으로 보여줘야 하는데, analyze.ts를 직접
// import하면 SDK가 클라이언트 번들에 섞여 들어간다.
export const LOCKED_PROMPT_RULES = `[카테고리 규칙]
grammar_points의 category는 반드시 다음 10개 중 하나만 사용하세요. 목록에 없는 이름을 새로 만들지 마세요.
조사_오용 / 경어_레벨_오류 / 어순_문제 / 시제_상_오류 / 활용형_오류 / 조수사_오류 / 어휘_선택_오류 / 생략_보충_오류 / 문형_오류 / 뉘앙스_오류

[여러 문장 처리]
아래에 문장이 번호대로 여러 개 주어집니다. reports 배열에 정확히 같은 개수(N개)를, 주어진 순서 그대로 담아 응답하세요. 문장끼리 서로 영향을 주지 않고 각각 독립적으로 분석하되, 위 [언어 규칙]은 1번째 문장이든 마지막 문장이든 예외 없이 똑같이 지키세요.

정의된 JSON 스키마 형식으로만 응답하고, 다른 설명 텍스트를 앞뒤에 붙이지 마세요.`;
